<h1>Burgers!</h1>

<h2>Eat these burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}

    {{/if}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers in your stomach!</h2>

{{!-- <ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers sleep=false}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add a Burger</button>
</form>
